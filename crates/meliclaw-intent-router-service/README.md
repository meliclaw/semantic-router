# HTTP facade for crate `meliclaw-intent-router`.

See the crate README and the repo-root README.

```bash
cargo run -p meliclaw-intent-router-service
# GET  /health
# POST /v1/route  {"query":"¿Cuál es el NIF del cliente Y?"}
```
